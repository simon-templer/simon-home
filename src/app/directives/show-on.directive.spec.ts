import { ShowOnDirective } from './show-on.directive';

describe('ShowOnDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowOnDirective();
    expect(directive).toBeTruthy();
  });
});
